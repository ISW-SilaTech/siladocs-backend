# ===================================================
# CONFIGURACIÓN UNIFICADA DEL PERFIL DE DOCKER
# ===================================================

spring:
  # 1. CONEXIÓN A POSTGRES
  datasource:
    url: jdbc:postgresql://postgres:5432/siladocs
    username: siladocs
    password: siladocs
    driver-class-name: org.postgresql.Driver
  
  # 2. CONFIGURACIÓN DE JPA (Hibernate)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # 3. CONFIGURACIÓN DE EMAIL (SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: alejandro.zarate.personal@gmail.com
    password: dnrc zaqe siud ixkz
    properties:
      mail.smtp:
        auth: true
        starttls.enable: true

# ===================================================
# CONFIGURACIONES PERSONALIZADAS (siladocs, app, blockchain)
# ===================================================

siladocs:
  # 4. CONEXIÓN A MINIO (S3)
  storage:
    endpoint: http://minio:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: siladocs
  # 5. CONFIGURACIÓN DE JWT
  jwt:
    secret: change_this_secret_for_dev_please_replace
    expiration-ms: 86400000

# 6. CONFIGURACIÓN DE URLS DE FRONTEND
app:
  frontend:
    reset-url: http://localhost:3000/authentication/reset-password/basic/

# 7. CONFIGURACIÓN BLOCKCHAIN (FABRIC - MIDDLEWARE)
blockchain:
  fabric:
    api:
      url: http://fabric-middleware:8000
      timeout:
        connect: 10000
        read: 30000
